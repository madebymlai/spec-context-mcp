#!/usr/bin/env node
/**
 * Post-install script that runs after npm install.
 * Never fails (always exits 0).
 */

function main(): void {
    // No post-install actions needed currently.
}

main();
process.exit(0);
